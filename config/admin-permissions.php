<?php

/**
 * Optional aliases for `crud.permission:{name}` when {name} should map to a different
 * Spatie permission prefix (e.g. plural route token → singular model-style name).
 */
return [
    'crud_resource_prefix' => [
        'shops' => 'shop',
    ],
];
