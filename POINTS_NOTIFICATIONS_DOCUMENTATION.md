# 🔔 Points System Notifications
## توثيق إشعارات نظام النقاط

---

## 📋 نظرة عامة

تم إضافة نظام إشعارات شامل لنظام النقاط. المستخدم يحصل على إشعار فوري عند:
- ✅ الحصول على نقاط
- ✅ استبدال النقاط
- ✅ انتهاء صلاحية النقاط

---

## 🎉 إشعارات الحصول على النقاط

### 1️⃣ مكافأة التسجيل (100 نقطة)

**متى يتم الإرسال:**
- عند تأكيد الـ OTP بعد التسجيل

**محتوى الإشعار:**
```json
{
  "title": "🎉 مرحباً بك!",
  "body": "تهانينا! حصلت على 100 نقطة كمكافأة تسجيل",
  "data": {
    "type": "points_earned",
    "points": 100,
    "reason": "user_registration"
  }
}
```

---

### 2️⃣ نقاط إتمام الطلب

**متى يتم الإرسال:**
- عند تغيير حالة الطلب إلى `delivered`

**محتوى الإشعار (طلب عادي):**
```json
{
  "title": "🎉 حصلت على نقاط!",
  "body": "تم توصيل طلبك بنجاح! حصلت على 100 نقطة\n\n✅ إتمام الطلب: 100 نقطة",
  "data": {
    "type": "points_earned",
    "points": 100,
    "order_id": 13,
    "breakdown": [
      "✅ إتمام الطلب: 100 نقطة"
    ]
  }
}
```

**محتوى الإشعار (أول طلب):**
```json
{
  "title": "🎉 حصلت على نقاط!",
  "body": "تم توصيل طلبك بنجاح! حصلت على 150 نقطة\n\n🎁 مكافأة أول طلب: 50 نقطة\n✅ إتمام الطلب: 100 نقطة",
  "data": {
    "type": "points_earned",
    "points": 150,
    "order_id": 13,
    "breakdown": [
      "🎁 مكافأة أول طلب: 50 نقطة",
      "✅ إتمام الطلب: 100 نقطة"
    ]
  }
}
```

**محتوى الإشعار (طلب كبير > 100):**
```json
{
  "title": "🎉 حصلت على نقاط!",
  "body": "تم توصيل طلبك بنجاح! حصلت على 300 نقطة\n\n✅ إتمام الطلب: 100 نقطة\n💰 مكافأة قيمة الشراء: 200 نقطة",
  "data": {
    "type": "points_earned",
    "points": 300,
    "order_id": 13,
    "breakdown": [
      "✅ إتمام الطلب: 100 نقطة",
      "💰 مكافأة قيمة الشراء: 200 نقطة"
    ]
  }
}
```

---

### 3️⃣ نقاط تقييم المنتج (10 نقاط)

**متى يتم الإرسال:**
- عند إضافة تقييم لمنتج

**محتوى الإشعار:**
```json
{
  "title": "⭐ شكراً على تقييمك!",
  "body": "حصلت على 10 نقطة مقابل تقييم المنتج",
  "data": {
    "type": "points_earned",
    "points": 10,
    "reason": "product_review",
    "review_id": 5
  }
}
```

---

## 💸 إشعارات استبدال النقاط

### 1️⃣ استبدال بكوبون خصم

**متى يتم الإرسال:**
- عند استبدال النقاط بكوبون خصم

**محتوى الإشعار:**
```json
{
  "title": "🎟️ تم استبدال النقاط!",
  "body": "تم استبدال 500 نقطة بكوبون خصم بقيمة 50",
  "data": {
    "type": "points_redeemed",
    "exchange_type": "coupon",
    "points_used": 500,
    "discount_amount": 50
  }
}
```

---

### 2️⃣ استبدال بتوصيل مجاني

**متى يتم الإرسال:**
- عند استبدال النقاط بتوصيل مجاني

**محتوى الإشعار:**
```json
{
  "title": "🚚 توصيل مجاني!",
  "body": "تم استبدال 300 نقطة بتوصيل مجاني لطلبك القادم",
  "data": {
    "type": "points_redeemed",
    "exchange_type": "free_delivery",
    "points_used": 300
  }
}
```

---

### 3️⃣ استبدال بهدية

**متى يتم الإرسال:**
- عند استبدال النقاط بهدية

**محتوى الإشعار:**
```json
{
  "title": "🎁 تم طلب الهدية!",
  "body": "تم استبدال 1000 نقطة بـ Gift Card $50. سيتم التواصل معك قريباً",
  "data": {
    "type": "points_redeemed",
    "exchange_type": "gift",
    "points_used": 1000,
    "gift_name": "Gift Card $50"
  }
}
```

---

## 📊 ملخص أنواع الإشعارات

| Event | Icon | Title | Points | When |
|-------|------|-------|--------|------|
| التسجيل | 🎉 | مرحباً بك! | 100 | عند verify OTP |
| أول طلب | 🎁 | حصلت على نقاط! | 50 | عند توصيل أول طلب |
| إتمام طلب | ✅ | حصلت على نقاط! | 100 | عند توصيل أي طلب |
| طلب كبير | 💰 | حصلت على نقاط! | 200 | طلب > 100 |
| تقييم منتج | ⭐ | شكراً على تقييمك! | 10 | عند إضافة تقييم |
| كوبون خصم | 🎟️ | تم استبدال النقاط! | - | عند الاستبدال |
| توصيل مجاني | 🚚 | توصيل مجاني! | - | عند الاستبدال |
| هدية | 🎁 | تم طلب الهدية! | - | عند الاستبدال |

---

## 🔧 التفاصيل التقنية

### طريقة الإرسال
الإشعارات يتم إرسالها عبر قناتين:
1. **FCM (Firebase Cloud Messaging)**: للإشعارات Push
2. **Database Notifications**: للإشعارات داخل التطبيق

### الملفات المعدلة
- `app/Listeners/AwardPointsListener.php` - إضافة إشعارات عند الحصول على نقاط
- `app/Services/PointExchangeService.php` - إضافة إشعارات عند الاستبدال
- `app/Services/User/UserService.php` - إضافة إشعار عند التسجيل

### الـ Data Payload
كل إشعار يحتوي على:
- `type`: نوع الإشعار (`points_earned` أو `points_redeemed`)
- `points`: عدد النقاط
- `reason` أو `exchange_type`: السبب
- بيانات إضافية حسب النوع

---

## 📱 للفرونت إند

### استقبال الإشعارات
```javascript
// FCM Notification Handler
messaging.onMessage((payload) => {
  const { title, body, data } = payload.notification;
  
  if (data.type === 'points_earned') {
    // عرض إشعار الحصول على نقاط
    showPointsEarnedNotification(data.points, data.reason);
  } else if (data.type === 'points_redeemed') {
    // عرض إشعار استبدال النقاط
    showPointsRedeemedNotification(data.exchange_type, data.points_used);
  }
});
```

### عرض الإشعارات
```javascript
function showPointsEarnedNotification(points, reason) {
  // عرض toast أو modal
  toast.success(`🎉 حصلت على ${points} نقطة!`);
  
  // تحديث رصيد النقاط في الواجهة
  updatePointsBalance();
}
```

---

## ✅ الخلاصة

النظام الآن يرسل إشعارات تلقائية للمستخدم في كل مرة:
- يحصل فيها على نقاط (تسجيل، طلب، تقييم)
- يستبدل فيها نقاط (كوبون، توصيل، هدية)

الإشعارات تحتوي على تفاصيل كاملة وواضحة للمستخدم! 🎉
